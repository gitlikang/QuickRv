<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.march.quickrv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.ItemHeaderAdapterTest" />
        <activity android:name=".test.TypeAdapterTest" />
        <activity android:name=".test.ClickListenerActivity" />
        <activity android:name=".test.HeaderFooterTest" />
        <activity android:name=".test.LoadMoreTest"></activity>
    </application>

</manifest>